# ====================================
# SwipeLP nginx設定
# ====================================
# ドメイン名を自分のドメインに変更してください
# 例: swipe.yourdomain.com

server {
    listen 80;
    server_name _;

    # Let's Encrypt SSL証明書取得用
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # HTTPSにリダイレクト（SSL設定後に有効化）
    # return 301 https://$host$request_uri;

    # ===== SSL設定前はこのブロックを使用 =====
    # アップロード画像の直接配信（nginx高速）
    location /uploads/ {
        alias /var/www/uploads/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # 静的ファイルのキャッシュ
    location ~* \.(css|js|jpg|jpeg|png|webp|gif|ico|svg|woff2)$ {
        proxy_pass http://app:3000;
        expires 7d;
        add_header Cache-Control "public";
    }

    # API & 動的ルート
    location / {
        proxy_pass http://app:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Beacon API対応
        proxy_set_header Connection "";
        proxy_buffering off;
    }
}

# ===== SSL有効化後はこのブロックのコメントを外す =====
# server {
#     listen 443 ssl http2;
#     server_name swipe.yourdomain.com;
#
#     ssl_certificate /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     # HSTS
#     add_header Strict-Transport-Security "max-age=63072000" always;
#
#     location /uploads/ {
#         alias /var/www/uploads/;
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
#
#     location ~* \.(css|js|jpg|jpeg|png|webp|gif|ico|svg|woff2)$ {
#         proxy_pass http://app:3000;
#         expires 7d;
#         add_header Cache-Control "public";
#     }
#
#     location / {
#         proxy_pass http://app:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header Connection "";
#         proxy_buffering off;
#     }
# }
